{
  "name": "evote-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for eVote blockchain application",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.ts --network amoy",
    "compile": "npx hardhat compile"
  },
  "devDependencies": {
    "hardhat": "^2.12.0",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "dotenv": "^16.0.0",
    "typescript": "^5.0.0"
  }
}
